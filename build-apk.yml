name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Find and extract project ZIP
        run: |
          ZIP=$(find . -maxdepth 3 -type f -name "*.zip" | head -1)
          echo "ZIP=$ZIP"
          unzip -o "$ZIP" -d extracted

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Build APK
        run: |
          PROJECT=$(find extracted -type f -name "build.gradle" -path "*/app/../build.gradle" | head -1)
          ROOT=$(dirname "$(dirname "$PROJECT")")
          cd "$ROOT"
          gradle assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Police-Duty-Rotation-APK
          path: "**/build/outputs/apk/debug/*.apk"
